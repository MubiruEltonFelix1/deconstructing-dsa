package dsa.sorting;

/**
 * HeapSort — in-place comparison-based sort using a binary heap.
 *
 * TODO: Implement max-heapify, build-max-heap, and extract-max loop.
 */
public class HeapSort {

    public void sort(int[] arr) {
        // TODO: build max-heap, then repeatedly extract the root
    }
}
