package dsa.searching;

/**
 * RangeBinarySearch — finds the leftmost and rightmost occurrence of a
 * target in a sorted array with duplicates (lower-bound / upper-bound).
 *
 * TODO: Implement lowerBound() and upperBound() helpers, then combine
 *       them in a range() method returning [lo, hi].
 */
public class RangeBinarySearch {

    public int[] range(int[] arr, int target) {
        // TODO: return [firstIndex, lastIndex] or [-1, -1] if not found
        return new int[]{-1, -1};
    }
}
