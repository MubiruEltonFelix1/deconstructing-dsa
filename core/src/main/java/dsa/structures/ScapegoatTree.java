package dsa.structures;

/**
 * ScapegoatTree — a self-balancing binary search tree with a tunable
 * balance factor alpha (0.5 < alpha < 1).
 *
 * TODO: Implement insert, delete, search, and rebuild (flatten + rebalance).
 */
public class ScapegoatTree {

    // TODO: fields (root, alpha, maxNodeCount, etc.) and public methods
}
