# Scapegoat Tree

## Overview

<!-- TODO: explain the weight-balanced BST concept, the alpha tuning
     parameter (0.5 < alpha < 1), the scapegoat-finding and rebuild
     (flatten + rebalance) mechanism, and the amortised O(log n) analysis. -->
