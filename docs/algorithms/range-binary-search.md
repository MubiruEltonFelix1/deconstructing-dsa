# Range Binary Search

## Overview

<!-- TODO: explain the lower-bound / upper-bound dichotomy, the use case
     for counting occurrences in a sorted array with duplicates, and the
     O(log n) complexity of both bounds. -->
